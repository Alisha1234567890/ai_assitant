/** Local guest session when auth is skipped or backend auth is unavailable */
export const GUEST_USER = {
  id: "user123",
  email: "guest@local.dev",
  name: "Guest",
};

export const GUEST_TOKEN = "guest-local-dev";
